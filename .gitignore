*.js
!jest.config.js
*.d.ts
node_modules

# CDK asset staging directory
.cdk.staging
cdk.out

# Local Lambda dependencies
app-code/node_modules/

.DS_Store

# Credentials — never commit
.env
.env.*
!.env.example

# Trained model artifacts: multi-MB float32 blobs, they live in S3, not git.
# Regenerate with ml/notebooks/two_tower_movielens.ipynb.
ml/artifacts/
ml-20m/
*.zip
# Archivos de bloqueo temporales de Word (se crean al abrir un .docx y no
# deben versionarse: son basura de sesion, no contenido).
~$*
